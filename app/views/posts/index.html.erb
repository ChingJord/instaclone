<% @posts.each do |p| %>
	<div class="imagebox">
	  <div class="image">
			<%= link_to image_tag(p.image.url(:medium)), post_path(p) %>
		</div>
		<div class="description">
			<%= p.description %>
		</div>
	</div>
<% end %>
