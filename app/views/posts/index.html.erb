<% @posts.each do |p| %>
	<div class="imagebox">
	  <div class="image">
			<%= image_tag(p.image.url(:medium)) %>
		</div>
		<div class="description">
			<%= p.description %>
		</div>
	</div>
<% end %>
